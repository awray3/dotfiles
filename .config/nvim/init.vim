set runtimepath^=~/.vim runtimepath+=~/.vim/after
let &packpath = &runtimepath
source ~/.vim/vimrc


"lua require'fterm'.config({
    "\ position="top",
    "\ width=100,
    "\ height=20,
    "\ commands=true})
